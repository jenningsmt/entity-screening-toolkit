# Core data engineering
duckdb
pandas
pyarrow

# Entity resolution / matching
rapidfuzz

# UI
streamlit

# HTTP / API access (NSF, OpenAlex, EDSM-style APIs; Streamlit -> API client)
requests

# Excel export (Epic G)
openpyxl

# API layer (Section 9a)
fastapi
uvicorn[standard]

# Testing (httpx2 required by starlette.testclient as of starlette 1.6 —
# plain httpx is deprecated there and triggers a warning on every test run)
pytest
httpx2

# Bibliometric topic-similarity layer (deferred VSS work, docs/plans/2026-09-01-vss-...)
# CPU-only torch wheel avoids pulling unneeded CUDA binaries:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers
