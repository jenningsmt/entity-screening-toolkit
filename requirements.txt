# Core data engineering
duckdb
pandas
pyarrow

# Entity resolution / matching
rapidfuzz

# UI
streamlit

# HTTP / API access (NSF, OpenAlex, EDSM-style APIs; Streamlit -> API client)
requests

# Excel export (Epic G)
openpyxl

# API layer (Section 9a)
fastapi
uvicorn[standard]

# Testing (httpx required by FastAPI's TestClient)
pytest
httpx
