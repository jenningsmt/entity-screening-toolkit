# Core data engineering
duckdb
pandas
pyarrow

# Entity resolution / matching
rapidfuzz

# UI
# Pinned (the rest of this file is not -- a full pin sweep is separate debt):
# .streamlit/config.toml pins the app to a dark theme, and an unpinned rebuild
# would resolve streamlit to whatever is newest on PyPI that day, shipping the
# theme config into an untested version. 1.63.0 is the version already running
# in the deployed image (read via `docker exec` over SSH, 2026-09-07).
streamlit==1.63.0

# HTTP / API access (NSF, OpenAlex, EDSM-style APIs; Streamlit -> API client)
requests

# Excel export (Epic G)
openpyxl

# API layer (Section 9a)
fastapi
uvicorn[standard]

# Epic J -- evidence-grounded explanation generation. Only
# entity_screening/explanation/generate.py imports this, and only inside
# _default_anthropic_call -- every other code path (including the whole
# test suite except test_explanation_real_model.py) never reaches it.
anthropic

# Testing (httpx2 required by starlette.testclient as of starlette 1.6 —
# plain httpx is deprecated there and triggers a warning on every test run)
pytest
httpx2

# NOT included here: torch / sentence-transformers, needed only for the optional
# --enrich-topic-similarity / --enrich-bibliometric-adjacent VSS layer (see
# requirements-vss.txt) -- deliberately kept out of the base install so every
# other feature (V1 through V3's core Epic E work) never forces a multi-hundred-MB
# ML dependency nobody asked for. See docs/plans/2026-09-01-vss-topic-similarity-layer.md.
